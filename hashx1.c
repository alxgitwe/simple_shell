#include "shell.h"

/**
 * hsh - function
 * @a: the struct
 * @av: the argument vector from main()
 * Return: return
 */
int hsh(infot *a, char **av)
{
	free(a);
	free(av);
	

/*
	while (b != -1 && c != -2)
	{
		infoclr(a);
		if (itrtive(a))
			print_prompt(a);
		erptchr(BUF_FLUSH);
		b = get_input(a);
		if (b != -1)
		{
			if (!infost(a, av))
			{
				c = fdblt(a);
				if (c == -1)
					fdcd(a);
			} else
				c = -3;
		}
		else if (itrtive(a))
			_putchar('\n');
		infofree(a, 0);
	}
	hstwt(a);
	infofree(a, 1);
	if (!itrtive(a) && a->st)
		exit(a->st);
	if (c <= -2)
	{
		if (a->ernm == -1)
			exit(a->st);
		exit(a->ernm);
	}*/
	return (1);

}
